# DECISIONS.md — Karar Günlüğü

> Planlama oturumlarında alınan kararlar ve gerekçeleri. Claude Code bir davranışın "neden böyle" olduğunu buradan doğrular. Yeni kararlar tarihiyle eklenir; karar değişirse eskisi silinmez, "revize edildi" notu düşülür.

## Oturum 1 — Kapsam ve Temel Mimari (2026-07)

| # | Karar | Gerekçe |
|---|---|---|
| D1 | Mobil uygulama ertelendi; her şey web tabanlı, push yerine panel içi sesli bildirim | Basit başlangıç; ileride PWA→native yolu açık |
| D2 | Ödeme: kasada + online (iyzico), tenant seçimli | Esneklik |
| D3 | Baştan multi-tenant SaaS | Satılabilir ürün hedefi; RLS izolasyonu temel |
| D4 | 3 personel yüzeyi: Admin + KDS + Garson paneli | Rol bazlı odaklı ekranlar |
| D5 | Stack: Next.js + Supabase | Hız + Realtime + RLS uyumu |
| D6 | Çok dilli altyapı; tenant dillerini seçer | Uluslararası satış kapısı |
| D7 | Süper Admin baştan; abonelik + **lifetime** + **self-hosted (yıllık lisans)** modelleri | Esnek gelir modelleri; lisans anahtarı altyapısı gerekir |
| D8 | Ürün yapısı: varyant + ekstra + stok | Zengin menü ihtiyacı |
| D9 | Tek premium tema ile başla; tema altyapısı public/**private (tenant'a özel)** atamalı | Müşteriye özel tema satabilme |
| D10 | Sipariş modu tenant ayarlı: `direct` / `approval` | Farklı işletme alışkanlıkları |
| D11 | Tam pazarlama sitesi (tanıtım+fiyat+kayıt) | SaaS satış kanalı |
| D12 | Detaylı raporlama | Karar destek + satış argümanı |

## Oturum 2 — Detaylandırma (2026-07)

### Tasarım
| # | Karar | Gerekçe |
|---|---|---|
| D13 | Tema kimliği: **`warm-luxury`** — lüks + sıcak karışımı (espresso/antrasit + krem + altın) | "Aşırı koyu olmasın" talebi; gündüz okunabilirlik |
| D14 | Ürün düzeni esnek: grid / liste / vitrin şeridi; **admin kategori başına seçer** (ayar Faz 2) | Her menü tipine uyum |
| D15 | Animasyon: dengeli mikro animasyonlar | Premium his + performans |

### Fiyatlandırma
| # | Karar | Gerekçe |
|---|---|---|
| D16 | 3 plan: Başlangıç / Pro / Sınırsız | Sade karar, klasik SaaS |
| D17 | Limit **yalnızca masa sayısı**; tüm özellikler her planda açık | En anlaşılır metrik |
| D18 | 14 gün tam özellikli trial, **kart istenmez** | Sıfır giriş bariyeri |

### Ödeme & Hesap
| # | Karar | Gerekçe |
|---|---|---|
| D19 | Hesap bölme fazlı: tek ödeme + eşit bölüşme (Faz 3), kalem bazlı (Faz 5); tenant hangilerini açacağını seçer | Riski dağıtma |
| D20 | Bahşiş: hazır tutar çipleri (50/100/200/300/400/500₺, **tenant düzenler**) + özel tutar | Hızlı seçim, yazma derdi yok |
| D21 | Varsayılan: KDV dahil net fiyat, sıfır ek ücret; **Pricing Rules motoru** altyapısı (servis ücreti, mutlu saat, paket farkı) tenant isterse aktive eder — temel motor Faz 5 | Şeffaflık + gelecek esnekliği |

### Sipariş Akışı
| # | Karar | Gerekçe |
|---|---|---|
| D22 | İptal karma: `pending/approved` serbest; `preparing` sonrası **iptal isteği** garson onaylı | Müşteri özgür, mutfak korunur |
| D23 | Oturum: otomatik açılış + ödeme/timeout/manuel kapama; **masa taşıma sadece garson yetkisi** (müşteride görünmez) | Kendi kendine düzelen sistem + saha ihtiyacı |
| D24 | Müşteri görünümü: tüm oturum siparişleri + ara toplam + Hesap İste + **cihaz bazlı etiket** | Şeffaflık; kalem bazlı bölmeye temel |

### Erişim & Onboarding
| # | Karar | Gerekçe |
|---|---|---|
| D25 | URL: subdomain + custom domain kapısı | Premium imaj + ileride üst plan özelliği |
| D26 | Giriş karma: admin e-posta+şifre; garson/mutfak **yetkili cihazda vardiya modu + PIN** | Güvenlik + saha hızı |
| D27 | Onboarding iki yol: **"Demo veriyle keşfet"** (toplu demo temizleme butonuyla) veya **"Sıfırdan kur"** tam sihirbazı (logo→dil→para birimi→masalar→şablon menü→tema önizleme) | Hem hızlı fikir hem temiz kurulum |

### Bildirim, QR, Dayanıklılık
| # | Karar | Gerekçe |
|---|---|---|
| D28 | Sesler olay tipine göre + cihaz başına ayar + kritik olaylarda **ısrarcı tekrar modu** | "Garson duymadı" senaryosunu öldürmek |
| D29 | **İki QR tipi:** masa QR'ı (doğrudan oturum) + genel QR (**masa seçtirir**); baskı şablonları çoklu format + tema uyumlu | Tek QR bastırma esnekliği + görsel bütünlük |
| D30 | Dayanıklılık: reconnect+senkron, bağlantı göstergesi, sepet koruması, idempotency | Kafe Wi-Fi gerçeği |

### Müşteri Deneyimi
| # | Karar | Gerekçe |
|---|---|---|
| D31 | Değerlendirme: dahili yıldız+yorum; 4-5★ → Google köprüsü, düşük puan içeride + admin uyarısı; **garson puanlama** (isim/yaka no/foto gösterimi tenant ayarlı) | Google puanı büyütme + personel ölçümü |
| D32 | Görseller: telifsiz hazır kütüphane + otomatik optimizasyon (WebP) | Boş menü olmasın + LCP hedefi |
| D33 | "Hazır" bildirimi: şimdi ses/titreşim+sekme uyarısı; Web Push Faz 6 | iOS sürtünmesini erteleme |
| D34 | KVKK/çerez/sözleşmeler + veri silme akışı kapsama alındı | Yasal zorunluluk |

### Operasyon
| # | Karar | Gerekçe |
|---|---|---|
| D35 | Çağrı tipleri: standart set + tenant özel tip + **tipsiz tek dokunuş çağrı** | Konsepte uyum + en hızlı yol |
| D36 | Para birimi: tenant tek birim seçer; çoklu kur gösterimi gelecek faz | Uluslararası kapı, düşük maliyet |
| D37 | Mutfak istasyonları: şema baştan hazır (`station`), v1 tek ekran, istasyon ekranları Faz 5 | Bugün basit, yarın hazır |

## Oturum 3 — 100k$ Kriter Analizi ve Kapsam Genişletme (2026-07)

### Ürün Anayasası
| # | Karar | Gerekçe |
|---|---|---|
| D38 | **Dinamik Ölçeklenme İlkesi + Modül Sistemi** (`tenant_modules`): çekirdek = QR menü; kasa, stok, CRM, kanallar, rezervasyon, kiosk, vardiya, hediye kartı vb. tenant'ın açıp kapattığı modüller; kapalı modül hiçbir yüzeyde görünmez | "Sadece QR menü isteyene de tam restoran yönetimi isteyene de hizmet" talebi |

### Şube
| # | Karar | Gerekçe |
|---|---|---|
| D39 | Şube: şema tam + görünmez tek şube + hazır seçici altyapısı | Migration acısını bugünden sıfırlama |
| D40 | Merkezi menü + `branch_product_overrides` (fiyat/stok/satılmıyor) | Zincir standardı |
| D41 | Plan: dahil şube sayısı + ek şube aylık ücret | Sade + ölçeklenen gelir |

### Kasa & Finans
| # | Karar | Gerekçe |
|---|---|---|
| D42 | Kasa modülü: vardiya kasa (açılış/sayım/fark) + **POS-lite sipariş girişi** + gün sonu snapshot (Faz 3); **donanım paketi ayrı fazda** (termal/çekmece/barkod, adaptör kapısı baştan) | QR kullanmayan müşteri gerçeği; donanım sürücüleri çekirdeği bekletmesin |
| D43 | İkram/indirim: izin bayrağı + **zorunlu sebep kodu** → kayıp-kaçak raporu | Suistimal görünürlüğü |
| D44 | İade fazlı: tam iade Faz 3 (online=iyzico gerçek iade, kasa=manuel); kısmi/kalem iade Faz 6 (hesap bölmeyle ortak altyapı) | Riskli karmaşıklığı fazlama |

### Stok & Maliyet
| # | Karar | Gerekçe |
|---|---|---|
| D45 | **Hibrit stok:** ürün başına `simple` / `recipe` modu; malzeme+reçete şeması baştan, reçete UI Faz 8 | Kolay başla, olgunlaş |
| D46 | Tedarik fazlı: basit alım+hareketli ortalama maliyet önce; tam PO ileri faz | Maliyet verisinin temeli |
| D47 | Kârlılık fazlı: manuel maliyet+marj erken (Faz 3); reçeteyle **otomatik maliyet + menü mühendisliği matrisi** (Faz 8) | Erken değer + imza rapor |

### CRM & Pazarlama
| # | Karar | Gerekçe |
|---|---|---|
| D48 | Müşteri kimliği: **telefon+OTP sadakat hesabı**, tamamen opsiyonel; menü anonim akmaya devam eder | Sürtünmesiz deneyim korunur |
| D49 | Sadakat: modüler motor — tenant damga/puan seçer; kademe ileri faz | Kafe kültürüne uyum |
| D50 | Kampanya fazlı: kural bazlı (Pricing Rules üstünde, Faz 6); segmentli+İYS iletişimi CRM sonrası | Aynı altyapı, doğru sıra |
| D51 | **AÇIK KARAR — Pazar yeri bağlantı biçimi:** adaptör mimarisi kesin; doğrudan platform API'leri mi (iş ortaklığı onayı gerekir) yoksa aracı katman sağlayıcısı mı — onay süreçleri/maliyet netleşince | API erişimleri izne tabi |
| D52 | Hediye kartı modülü plana alındı (bakiye=borç muhasebesiyle) | Ön nakit + sadakat sinerjisi |

### Kanallar
| # | Karar | Gerekçe |
|---|---|---|
| D53 | `order_channel` şeması baştan; gel-al → kendi kurye paketi + zamanlanmış sipariş fazlı (Faz 9) | Tek sipariş motoru |
| D54 | Kurye: `courier` rolü şimdi; basit modül Faz 9; canlı takip gelecek kapısı; **modül olarak aktif/pasif** | Kuryesiz işletme zorlanmaz |

### Analitik
| # | Karar | Gerekçe |
|---|---|---|
| D55 | Ayrı **Analitik Merkezi**: widget dashboard + hedefler/anomali uyarıları + **zamanlanmış e-posta/PDF raporları** | Rapor → karar asistanı |
| D56 | Rapor erişimi rol/izin bayraklı; **veri geçmişi sınırsız** + dönem/geçen yıl kıyası | Kurumsal beklenti |

### Yetki & Platform
| # | Karar | Gerekçe |
|---|---|---|
| D57 | İzin sistemi: sabit roller + **tenant ayarlı izin bayrakları** (ikram, iade, raporlar, kasa, masa taşıma...); özel rol oluşturma gelecek | %90 ihtiyaç, sade model |
| D58 | 2FA: TOTP altyapısı; **zorunluluk Süper Admin anahtarında, varsayılan opsiyonel** (öneri: canlıda süper admin için açılması) | Kullanıcı talebi: dayatma yok |
| D59 | Status sayfası + planlı bakım duyuruları + panel banner | Kesinti güveni |
| D60 | Ortamlar: Prod+Staging+PR preview; CI (tip/lint/test/RLS) zorunlu; yedek+PITR+aylık restore tatbikatı (OPERATIONS.md) | Profesyonel işletim |
| D61 | Muhasebe fazlı: CSV/Excel + program uyumlu exportlar erken; doğrudan API adaptörü Faz 10; **ÖKC adaptör kapısı** mimaride | TR satış gerçeği |
| D62 | Tenant API fazlı: API key + imzalı webhooks + read API (Faz 10); yazma API'si pazar yeri altyapısıyla | Entegre edilebilir ürün |

### Ek Modüller
| # | Karar | Gerekçe |
|---|---|---|
| D63 | Plana alındı: **Rezervasyon+bekleme listesi, Kiosk modu, Vardiya planlama+puantaj** (Faz 11) — hepsi modül | 100k$ kapsam |
| D64 | **AÇIK KARAR — Wi-Fi portalı:** kararsız; OTP'li sadakat aynı veriyi topluyor; ileride konuşulacak | Donanım+KVKK yükü |

## Oturum 4 — Açık Kararların Kapatılması (2026-07)

| # | Karar | Gerekçe |
|---|---|---|
| D65 | Destek: **panel içi ticket modülü** (tenant talep → Süper Admin kuyruğu, durum akışı); Yardım Merkezi gelecek kapısı | Talepler tek yerde, ölçülebilir |
| D66 | **REVİZE — SMS sağlayıcısı:** adaptör mimarisi kesin (`lib/integrations/sms`), altyapı Faz 7'de kurulur; somut sağlayıcı seçimi/entegrasyonu artık zorunlu değil, kullanıcı gündeme getirdiğinde yapılır (gerekirse Faz 12 havuzuna kadar ertelenebilir) | Kullanıcı talebi (2026-07-19): OTP/SMS şu an öncelik değil, son iş olarak bakılacak |
| D67 | Geliştirme akışı: **lokal öncelikli** (Docker + Supabase CLI, her şey lokalde görülür/test edilir) → GitHub+CI → canlıya alma **Vercel + Supabase Cloud**; Docker image üretimi baştan (self-hosted paketin temeli) | Kullanıcı talebi: lokalde görme + kontrollü canlıya çıkış |
| D68 | Plan taslağı: **Başlangıç 10 masa/1 şube · Pro 25/2 · Sınırsız ∞/3 dahil** + ek şube ücreti; fiyatlar lansmanda | Dengeli segmentasyon |
| D69 | Wi-Fi portalı **kapsam dışı** (D64 kapandı) | OTP'li sadakat aynı veriyi topluyor; donanım+KVKK yükü |
| D70 | Pazar yeri **karma strateji** (D51 kapandı): aracı katmanla (Posentegra/Entegre App/API Merkezi sınıfı) hızlı çıkış + Yemeksepeti resmî Plugin ve Trendyol partner-bilgisi doğrudan bağlantılarına kademeli geçiş; adaptör deseni geçişi şeffaf kılar | Araştırma bulguları: YS dokümante resmî program, Trendyol self-servise yakın, aracılar oturmuş pazar |
| D71 | **REVİZE — Ürün ismi + domain:** Ürün adı **RKYS Dashboard** olarak belirlendi. Alan adı henüz alınmadı; kullanıcı ileride alacak — müsait değilse başka bir isim seçilip dokümanlar/koddaki referanslar güncellenecek. Subdomain/custom domain mimarisi (D25) zaten hazır, isim değişikliği mimariyi etkilemez | Kullanıcı kararı (2026-07-19) |

## Oturum 5 — Repo Kurulumu (2026-07)

| # | Karar | Gerekçe |
|---|---|---|
| D72 | **Production'a asla erken çıkılmaz:** Proje **lokalde uçtan uca tamamlanmadan** (tüm fazlar bitip kullanıcı onayı alınmadan) hiçbir şekilde Vercel/Supabase Cloud production ortamına deploy yapılmaz; staging dahi kullanıcı onayı ile açılır. Bkz. RULES.md #45 | Kullanıcı açık talimatı (2026-07-19): canlıya çıkış tamamen kullanıcı kontrolünde, aceleye getirilmeyecek |

## Oturum 6 — Test Ritüeli Otomasyonu (2026-07)

| # | Karar | Gerekçe |
|---|---|---|
| D73 | **REVİZE — Faz kapanış testi otomatik:** Manuel el testi ritüeli (TESTING.md §5, eski RULES #43) kaldırıldı. Faz kapanışında eskiden manuel listede yer alacak senaryolar Playwright E2E testine dönüştürülür; Claude Code tam paketi (unit+entegrasyon+E2E) koşturup sonucu sohbette özetler. Gerçek cihaz/görsel kontrol otomatikleştirilemediği için bloklayıcı değildir. **Fazlar arası geçiş yine de her zaman kullanıcı onayına tabidir** — bu değişmedi. Faz içindeki uygulama adımları için ayrıca onay beklenmeyecek (bu gevşetme geçicidir, **Faz 4'te tekrar değerlendirilecek**). | Kullanıcı talebi (2026-07-20): manuel test istemiyorum, otomatik doğrulama yeterli; onay eşiği sadece faz geçişinde kalsın |
| D74 | **EK — Faz kapanışında iki adımlı görünürlük+onay:** D73'ün otomasyonu korunur, ama faz kapanışında Claude Code önce eski manuel-test-listesi formatında okunabilir bir senaryo listesi sunar, ardından açıkça koşum onayı ister ("bunları siz test edebilirsiniz, ya da onay verirseniz sizin yerinize ben yaparım"). Onay sonrası otomatik paket çalışır ve sonucu özetlenir; paket yeşilse **ayrıca** faz geçiş onayı istenir (D73'teki onay adımından bağımsız, ikinci bir onay noktası). | Kullanıcı talebi (2026-07-20): listeyi görmek istiyorum ama test yine otomatik kalsın; "test edebilirsiniz, onay verirseniz ben yaparım" şeklinde ilerlensin |
| D75 | **EK — Adım bazlı otomatik commit+push:** Bir plandaki her numaralı Adım (Faz 1'deki Adım 0-7 gibi) bitip o adımı ilgilendiren hızlı doğrulama (tsc + lint + ilgili unit/entegrasyon testleri) yeşil olunca, Claude Code **onay beklemeden** commit atar ve `origin/main`'e push eder — bu, genel "kullanıcıdan onay almadan commit/push yapma" varsayılanının bu proje için kalıcı istisnasıdır. Commit Kuralları (küçük/odaklı, migration'lar ayrı commit, Conventional Commits) aynen uygulanır; bir Adım içinde migration + kod ikisi de değiştiyse ayrı commit'ler halinde push edilir. Tam paket (integration ×2 + E2E) her Adım'da değil, yalnızca **faz kapanışında** (D73/D74 ritüeli) koşulur — Adım seviyesinde hız önceliklidir. Faz kapanışındaki commit+push zaten D73/D74 akışının parçası, ayrıca onay gerekmez. | Kullanıcı talebi (2026-07-20): "her faz'ın her adımı tamamlandığında commit ve push işini halletsin" — Faz 1 sonuna kadar bu otomatik değildi, yalnızca faz sonunda ayrı onayla yapılıyordu; kullanıcı bunu Adım seviyesine indirmek istedi. Birim="her Adım sonunda" (alt-parçalara bölünmedi), push eşiği="yerel hızlı doğrulama" (tam paket değil) — kullanıcı seçimi. |
| D76 | **REVİZE — D73'ün "faz içi Adım onayı beklenmez" gevşetmesi Faz 4'ten itibaren kaldırıldı:** Faz 4'ten başlayarak bir plandaki her Adım bitip hızlı doğrulaması (ve varsa D75 otomatik commit+push'u) tamamlandıktan sonra, bir sonraki Adım'a geçmeden **önce** kullanıcıya durum özeti sunulur ve açık onay istenir — Faz 1-3'te uygulanan "adımlar arası dur, otomatik devam et" modeli sona erdi. D75'in Adım-sonu otomatik commit+push davranışı bu karardan etkilenmez (tamamlanan Adım yine onaysız commit'lenir); onay yalnızca **bir sonraki Adım'ın uygulamasına başlamadan önce** aranır. D73/D74'ün faz-kapanışı ritüeli (tam paket + iki adımlı görünürlük+onay) değişmeden devam eder. | Kullanıcı talebi (2026-07-21): "faz 4 için adımlar arası bi dur onay al sonra devam et" — D73 zaten bu noktanın Faz 4'te yeniden değerlendirileceğini not etmişti, kullanıcı D75'in Faz 4'ten itibaren tekrar durup onay istenmesini seçti. |
| D77 | **GEÇİCİ — D76'nın adımlar-arası onay gevşetmesi yalnızca Faz 4'ün kalanı için askıya alındı:** Faz 4 Adım 0-3 push edildikten sonra kullanıcı, Faz 4 bitene kadar Adım 4-8'in uygulama adımları arasında durup onay istenmemesini talep etti — D75'in Adım-sonu otomatik commit+push'u aynen sürer, yalnızca "bir sonraki Adım'a geçmeden önce onay" ara adımı Faz 4 tamamlanana kadar kaldırıldı. D73/D74'ün faz-kapanışı ritüeli (senaryo listesi → koşum onayı → tam paket → **ayrı** faz geçiş onayı, Adım 8'de) bundan etkilenmez, aynen uygulanır. Faz 5 başında D76'nın adımlar-arası onay davranışı varsayılan olarak geri döner — kullanıcı aksini söylemedikçe. | Kullanıcı talebi (2026-07-23, Adım 3 sonrası): "senden faz4'ü tamamlayana kadar durmamanı istiyorum. Adımlar arası onay alma commit push işlemlerini yap ama adımları otomatik geç." |
| D78 | **REVİZE — D76 kalıcı olarak kaldırıldı, adımlar arası onay kalıcı olarak sona erdi:** Faz 5 Adım 1 sırasında kullanıcı, D77'deki gibi tek bir faza özgü bir istisna değil, genel ve süresiz bir talimat verdi: bundan böyle **hiçbir fazda** Adım'dan Adım'a geçerken onay beklenmeyecek — Claude Code bir Adım bitip D75'in hızlı doğrulaması+otomatik commit+push'u tamamlanır tamamlanmaz bir sonraki Adım'ın uygulamasına kendiliğinden geçer (Faz 1-3'teki orijinal modele kalıcı dönüş). D73/D74'ün faz-kapanışı ritüeli (senaryo listesi → koşum onayı → tam paket → **ayrı** faz geçiş onayı) bu karardan etkilenmez, değişmeden sürer — kullanıcının talimatı yalnızca "faz adımları" ile sınırlıydı, fazlar arası geçişten bahsetmedi. | Kullanıcı talebi (2026-07-23, Faz 5 Adım 2 başlangıcında): "Faz adımlarında onay almadan devam et." — D77'nin aksine bir faza özgü değil, süresiz ve genel bir talimat. |

## Oturum 7 — Hız Önceliği (2026-07)

| # | Karar | Gerekçe |
|---|---|---|
| D79 | **REVİZE — D73/D74'ün fazlar arası geçiş onayı kalıcı olarak kaldırıldı:** Faz 7 kapanışı sırasında kullanıcı, D78'in adım-seviyesi onay kaldırmasının ötesine geçen genel ve süresiz bir talimat verdi: bundan böyle **fazlar arası geçişte de** onay beklenmeyecek — bir fazın tam test paketi (unit+entegrasyon×2+E2E) yeşil olduğunda Claude Code sonuç özetini sohbette paylaşıp **doğrudan bir sonraki fazın planlama/uygulamasına geçer**. D73/D74'ün geri kalanı (senaryo listesi sunumu, koşum onayı istenmesi, tam paketin koşturulması, sonuç özeti) **değişmeden sürer** — yalnızca en sondaki "ayrı faz geçiş onayı" adımı kaldırıldı. Orta/büyük yeni fazlarda CLAUDE.md'nin "önce plan, sonra kod" ilkesi ve otonom karar eşiği (mimari/kapsam/geri dönüşü zor işlemler için durup sorma) aynen geçerli — bu karar yalnızca "fazı kapat, bir sonrakine geç" onay noktasını kaldırıyor. | Kullanıcı talebi (2026-07-24, Faz 7 kapanışında): "Fazlar arası artık soru sorma projei hızlı bitirmek için fazlarıda onay almadan direk geç." — projeyi hızlı bitirme önceliği, D78'in mantığının fazlar arası geçişe genişletilmesi. |

## Oturum 8 — Faz 4 Revizyonu: Kayıt Onayı + Plan-Modül Yönetimi (2026-07)

| # | Karar | Gerekçe |
|---|---|---|
| D80 | **Kayıt kapalı-kapı onayı + otomatik/manuel ayarı:** `/kayit` formu artık tenant'ı doğrudan `active` açmıyor — `pending_approval` ile açılıyor, `plan_id` seçilen plana atanıyor, ödeme kök domainde (`/kayit/odeme`) alınıyor. `proxy.ts`'in mevcut `tenant_status !== 'active'` kapısı (0002) hiçbir ek mantık yazılmadan bu yeni durumu da (admin/login dahil) otomatik kapatıyor. Onay, platform admin'in elle `approve_tenant`/`reject_tenant` kararına VEYA genel `platform_settings.auto_approve_registrations` ayarına (varsayılan: kapalı/manuel) bağlı — ayar açıksa ödeme onaylanır onaylanmaz (`activate_subscription` içinde) tenant otomatik `active` olur ve planının modülleri açılır. | Kullanıcının fark ettiği güvenlik/monetizasyon açığı: herkes anında tam erişimli hesap açabiliyordu, plan seçtirilmiyordu. |
| D82 | **Rakip analizi sonrası yol haritası (Faz 13-18):** 5 rakip site (adisyo, Menulux, Orion POS, robotPOS, Protel) incelendi; bulgular pazarlama sitesi yeniden yapılanması (Faz 13-14) + ücretli ek modül ekonomisi (Faz 15) + personel motivasyonu/kurye canlı takip (Faz 16) + ÖKC adaptör kapısı (Faz 17) + bayilik kanalı (Faz 18) olarak PLAN.md'ye eklendi. Otel PMS entegrasyonu, servis robotu distribütörlüğü, tenant'lar arası ortak sadakat ağı (RLS tenant-izolasyon mimarisine doğrudan aykırı) ve kendi komisyonsuz pazaryerimiz kullanıcı onayıyla **bilinçli kapsam dışı** bırakıldı. D68'in "kesin fiyatlar lansmanda" notu Faz 13 Adım 0 ile kapandı — ana sayfa artık `plans`/`plan_modules`'tan gerçek fiyat ve modül listesi gösteriyor. | Kullanıcı talebi (2026-07-27): rakip sitelerin hepsini incele, bizde olan/olmayanı çıkar, "mükemmel site" + eksik özellikleri planla. |
| D81 | **Plan-modül şablonu + üç kaynaklı modül modeli (plan/paid_addon/granted) + düşürme incelemesi:** `plan_modules` tablosu her planın varsayılan modüllerini tanımlar (Süper Admin `/platform/plans`'tan serbestçe düzenler, `plans.key` artık sabit 3 değere kilitli değil). `tenant_modules.source` bir modülün nereden geldiğini ayırt eder: `plan` (plandan otomatik), `paid_addon` (ayrıca ücretli, düşürmede asla dokunulmaz), `granted` (platform admin elle vermiş veya bir "Talep Et" talebi onaylanmış). Plan değişikliği (platform admin ataması veya tenant'ın kendi ödeyerek yaptığı değişiklik, ikisi de paylaşılan `apply_plan_change` üzerinden) düşürmede plandan kaybolan modülleri hemen silmez — `pending_removal_since` ile işaretler; platform admin `resolve_pending_module_removal` ile Koru (source='granted' olur, kalıcı korunur, bir daha hiç işaretlenmez) veya Kaldır (`is_enabled=false`) kararı verir. Tenant kendi panelinde plan dışı bir modülü `request_module` ile talep edebilir, platform admin `resolve_module_request` ile onaylar/reddeder. **Varsayım:** modül adları için ayrı `platform.modules.<key>` i18n anahtarı açmak yerine mevcut `admin.settings.modules.keys.<key>` çevirileri tekrar kullanıldı (tek kaynak, düşük risk, kolayca revize edilebilir). | Kullanıcının onayladığı model: yükseltmede sorun yok, düşürmede ücreti ödenmiş modül kalıcı korunur; plan dışı modül isteğe bağlı talep akışıyla açılabilir. |
| D84 | **REVİZE — D61'in ÖKC adaptör kapısı "yalnızca arayüz" kararı mock-first'e genişletildi (Faz 17, S67).** D61 gerçek GİB sertifikasyonu alınamayacağı gerekçesiyle yalnızca `lib/integrations/fiscal/provider.ts` arayüzünün dokümante edilmesini, implementasyon (mock dahil) YAPILMAMASINI öngörüyordu. Plan yazımı sırasında bunun yerine payments/subscriptions ile birebir aynı mock-first deseni önerdim — bu D61 ile çelişiyordu, kullanıcıya soruldu ve genişletme onaylandı: `mock.ts`/`index.ts` eklendi, `fiscal_receipts` denetim izi tablosu + `get_fiscal_daily_summary` RPC'si + yeni `fiscal_integration` modülü (`tenant_modules`/`plan_modules`/`module_requests`/`module_addon_prices` check constraint'lerine eklendi) kuruldu, `cashier/pay/actions.ts`'teki `record_payment` akışına gerçekten bağlandı (best-effort, ödemeyi asla bloklamaz). Gerçek bir ÖKC sağlayıcısı hâlâ yok. | Kullanıcı onayı (2026-07-28): "Mock-first deseni genişlet" — D61'in test-edilemezlik gerekçesi geçerliliğini koruyor ama mock katmanı payments/subscriptions'la tutarlılık ve gerçek `record_payment` entegrasyonu için değerli görüldü. |
| D83 | **REVİZE — D72'nin erken kapanışı: Vercel + Supabase Cloud bağlantısı bilerek şimdi açıldı.** Proje henüz lokalde uçtan uca tamamlanmamışken (Faz 15 kapanmış, Faz 16 başlıyor), kullanıcı D72/RULES #45'in "production'a erken çıkış yasak" kuralını bilerek ve açıkça devre dışı bırakıp Vercel projesini (`rkys`, GitHub main dahil otomatik deploy) ve Supabase Cloud projesini (`rkys`, ref `ifwzdjiwvpkbzeofaxyj`, region ap-southeast-2) bağlattı. Süreçte iki gerçek migration hatası bulunup düzeltildi (bkz. commit `35c15ea`): (1) `0029_cash_shifts.sql` — `gen_random_bytes()` bare çağrıları Cloud'da `extensions` şeması search_path'te olmadığı için patlıyordu, `extensions.gen_random_bytes(...)` olarak nitelendi; (2) `0065_webhooks.sql` — `pg_net` eklentisi hiçbir migration'da resmi `create extension` ile açılmamıştı (yerelde ön yüklü geliyordu), eklendi. Tüm 83 migration Cloud'a başarıyla push edildi. Vercel env (Production+Preview): Supabase URL/anon/service-role key, `NEXT_PUBLIC_ROOT_DOMAIN=rkys.vercel.app`, yeni üretilen `INTERNAL_API_SECRET`; `LICENSE_SIGNING_PRIVATE_KEY` kullanıcı tarafından ayrıca eklenecek (mevcut embedded public key — `verify.ts` — ile eşleşmesi gerektiği için yeni çift üretilmedi). Staging ortamı bu kararın kapsamında DEĞİL — hâlâ kurulmadı, ayrı onay gerektirir. | Kullanıcı talebi (2026-07-28): "projemize vercel'i bağlayalım" — netleştirme sorularında D72'yi bilerek tetikleyen "main dahil tam bağlantı" ve "Supabase Cloud'u da şimdi açalım" seçenekleri onaylandı. |
| D85 | **Onboarding'e çoklu demo menü şablonu (kafe/restoran/pastane) eklendi, görseller ücretsiz stok arama ile bulunup Storage'a yüklendi (AI üretimi altyapısı hazır olana kadar geçici).** Önceki tek sabit şablon (1 kategori + 1 ürün, görselsiz) yerine `lib/onboarding/menuTemplates.ts`'te 3 statik şablon (her biri 4 kategori × 4 ürün, TR/EN ad+açıklama+fiyat) tanımlandı; hem "Demo veriyle keşfet" giriş ekranı hem de "Sıfırdan kur" sihirbazının "Şablon Menü" adımı artık tek buton yerine 3 şablon kartı gösteriyor (`TemplatePicker` bileşeni, ikisi arasında paylaşılan). 48 ürün görseli Openverse (CC lisanslı, `license_type=commercial`) üzerinden anahtar kelime aramasıyla bulunup mevcut `optimizeMenuImage` (wasm-vips) pipeline'ıyla webp'ye çevrildi, `menu-images` bucket'ının paylaşılan `_templates/<key>/<slug>.webp` alt yoluna service-role ile yüklendi — tenant-prefix RLS kısıtı bu path'i etkilemiyor çünkü yükleme RLS'i bypass eden service-role ile yapıldı, okuma zaten bucket geneli public. İlk arama turunda 11 görsel kalite sorunu (yanlış eşleşen içerik, rakip marka logosu görünen fotoğraf — ör. "Costa" bardağı, amatör/uygunsuz çekim) tek tek incelenip daha isabetli arama terimleriyle değiştirildi. **Gerçek AI görsel üretimi altyapısı henüz kurulu değil** (`GEMINI_API_KEY` yok, `google-genai` paketi kurulu değil) — kullanıcı bu görselleri şimdilik geçici/deneme kabul edip AI altyapısı hazır olunca değiştirmeyi tercih etti. Görseller yalnızca **local** Supabase Storage'a yüklendi, henüz Cloud'a senkronize edilmedi (D83 ile bağlanan prod projeye ayrı bir adım gerekir). | Kullanıcı talebi (2026-07-28): yeni müşterilerin demo hesabında lüks bir kafe/restoran menüsü + görseller görmesini istedi; AI üretimi altyapı eksikliği nedeniyle "sen direkt internetten uygun resim bul... deneme yapıyoruz, sonra değiştiririz" onayıyla stok görsel arama yaklaşımına geçildi, ardından "48 görselin tamamını tek tek incele" talebiyle kalite kontrolü genişletildi. |

## Oturum 9 — Bug-Hunt (2026-08-01)

| # | Karar | Gerekçe |
|---|---|---|
| D86 | **QR token'ların ham hâli artık AES-256-GCM ile şifrelenip AYRICA (hash'in yerine değil) saklanıyor — "QR Göster" (fiziksel QR'ı bozmadan tekrar görüntüleme) mümkün oldu.** Önceki tasarım yalnızca `qr_token_hash` (SHA-256) saklıyordu; bu, kod incelemesinde "RULES #7" diye anılıyordu ama RULES.md #7 aslında yalnızca token'ların *tahmin edilemez* üretilmesini şart koşuyor, ham değerin hiç saklanmamasını değil — o kısıt yalnızca bir uygulama tercihiydi, yazılı bir kesin kural değildi. `tables`/`generic_qr_codes`'a `qr_token_encrypted` (text, nullable) kolonu eklendi (migration 0088); şifreleme/çözme `lib/qr/token.ts`'te `QR_TOKEN_ENCRYPTION_KEY` env değişkeniyle (LICENSE_SIGNING_PRIVATE_KEY ile aynı desen — yalnızca env, asla commit) yapılıyor, anahtar yoksa kolon null kalır ve mevcut oluşturma/yenileme akışları etkilenmez (geriye dönük uyumlu, kırılma yok). Yeni `revealTableQr`/`revealGenericQr` server action'ları (staff-only) şifreli kopyayı çözüp döndürüyor; UI'da mevcut "QR'ı Yenile" butonunun yanına "QR Göster" eklendi. **GÜNCELLEME (2026-08-04):** anahtar prod'da (Vercel) **ayarlandı** — `vercel env ls production` ile doğrulandı, `QR_TOKEN_ENCRYPTION_KEY` Production kapsamında mevcut ve sonraki build'ler tarafından alındı. Bu satır 2026-08-01'de "henüz ayarlanmadı" diyordu ve eskimişti; 2026-08-04'te bu nota güvenip kullanıcıya yanlış bilgi verildi. Anahtar hiç ayarlanmasaydı davranış yine kırılma değil, "revealUnavailable" mesajı olurdu. | Kullanıcı onayı (bug-hunt oturumu, 2026-08-01): "Ham token'ı saklayıp gerçek 'tekrar göster' ekle" seçildi — QR yenilemenin eski fiziksel kodu geçersiz kılması pratikte rahatsız ediciydi. |
| D87 | **Garson artık kendi PIN'iyle, owner'ın admin oturumundan tamamen bağımsız giriş yapabiliyor — owner çıkışı garsonu düşürmüyor.** Kök neden: garson ve owner AYNI tarayıcıda AYNI Supabase Auth cookie'sini paylaşıyordu, ayrıca garson için hesap oluşturma akışı da hiç yoktu. Çözüm üç parça: (1) **Yeni personel oluşturma** (`createStaffMember`, staff/actions.ts) — artık `/admin/staff`'ta rol+rozet+PIN girilip service-role ile gerçek bir `auth.users` hesabı (sentetik, hiçbir yüzeyde görünmeyen e-posta) + `profiles` satırı açılıyor. (2) **`verify_staff_pin_identity` RPC** (migration 0089) — mevcut vardiya cihaz+PIN altyapısını (0026/0070) yan etkisiz, `staff_scheduling` modülünden bağımsız şekilde tekrar kullanır. (3) **İki bağımsız Supabase oturumu, AYNI tarayıcıda:** `/waiter/login` PIN doğrulanınca `auth.admin.generateLink` + `verifyOtp` ("parolasız giriş") ile o personelin GERÇEK oturumunu açar, ama **farklı bir cookie adı altında** (`sb-waiter-auth-token`, `lib/supabase/waiterClient.ts`) — `custom_access_token_hook` (0006) her iki cookie için de aynı şekilde çalıştığından RLS/RPC katmanına HİÇ dokunulmadı. `lib/supabase/server.ts`'in `createClient()`'ı artık iki cookie'den hangisinde geçerli bir PERSONEL oturumu varsa onu döner; `/waiter` altında (yalnızca orada, `proxy.ts`'in `x-rkys-prefer-waiter-session` header'ıyla) garson cookie'si önceliklidir, diğer TÜM yüzeylerde (admin/cashier/kitchen/misafir) davranış birebir korunur. Bu sayede `lib/data/*` ve `waiter/actions.ts` içindeki tek bir satır bile değişmeden hem admin hem PIN oturumuyla doğru çalışıyor. | Kullanıcı onayı (bug-hunt oturumu, 2026-08-01): "Tam çözümü şimdi yap" — araştırma sırasında keşfedilen gerçek boyut (RLS'nin gerçek JWT'ye bağımlılığı + personel oluşturma akışının hiç var olmaması) netleştirilip onaylandıktan sonra uygulandı. |

## Oturum 10 — Frontend Yeniden Tasarımı (2026-08-03)

| # | Karar | Gerekçe |
|---|---|---|
| D88 | **REVİZE — D9/D13'ün yerine geçer. Tema mimarisi üç katmana ayrıldı; `warm-luxury`/`sage-bistro` emekliye ayrıldı, yerlerine `gece`/`kagit`/`kor` geldi.** Önceki tasarımda `data-theme` kök `layout.tsx`'te `<html>`'e basılıyordu ve `globals.css` tüm tema token dosyalarını global import ediyordu — sonuç: tenant'ın seçtiği **menü teması admin panelini, KDS'i, garson panelini ve kasayı da boyuyordu** (bir tenant "Sage Bistro" seçince admin yeşile dönüyordu). Yeni model: **Katman 1** tenant teması yalnızca `data-surface="guest"` altında (`(menu)`), **Katman 2a** pazarlama (açık zemin, gölgeyle yükseklik, gradient/pastel serbest), **Katman 2b** uygulama chrome'u (çift modlu, düz, nötr, dolgu açıklığıyla yükseklik) — 2a ve 2b marka primitiflerini (petrol paleti, tipografi, yarıçap, hareket) paylaşır, yüzey işlemesini paylaşmaz. Kapsamlama `<html data-surface data-theme data-mode>` üzerinden; `data-surface` `proxy.ts`'te pathname'den türetilir (D87'nin `/waiter` header deseniyle aynı), `data-mode` cookie'den okunur (localStorage değil — ilk boyamada flaş olmasın). **Sarmalayıcı `<div>` kullanılmadı** çünkü `dropdown-menu`/`select`/`sheet` Base UI Portal ile `document.body`'ye render oluyor ve token'ları kaybederdi. Sızıntı üç katmanda yakalanır: lint (tenant tema dosyaları yalnızca `[data-surface="guest"]` seçicisi yazabilir), tip (`TenantToken`/`ProductToken` ayrı union) ve E2E (admin'de tema değişimi `--primary`'yi değiştirmez). Üç yeni tema **aynı bileşen setini** kullanır, yalnızca token'ları (renk, tipografi ölçeği, yarıçap, yoğunluk, hareket) değişir — tema başına ayrı bileşen yasak. Marka rengi **petrol/teal**: üç tenant vurgusundan (amber/altın/kırmızı) ayrışması gerekiyordu, aksi halde pazarlama sayfasında ürün ekran görüntüsü marka chrome'uyla yarışıyor. Erişilebilirlik hedefi AA + Kâğıt gövde metninde AAA (o temanın varlık sebebi güneş altında okunmak). **Ölçümle çıkan kısıt:** Kor'un doygun kırmızısı, siyah zeminde okunacak kadar açık ve üstünde beyaz metin taşıyacak kadar koyu aynı anda olamıyor (her doygunlukta beyaz CTA metni ≈3.9:1'de takılıyor) — `--accent` `L .615`'e açıldı, `--accent-fg` koyuya çevrildi (4.68:1). Migration 0090 mevcut tenant'ların **ikisini de Kâğıt'a** eşler (ikisi de açık tema; hiçbir işletmenin menüsü bir gecede siyaha dönmesin), yeni tenant varsayılanı `gece` olur. Tema adları için yeni kolon açılmadı — D81'in deseni tekrar kullanıldı: UI `t('admin.settings.themes.<key>')` anahtarını dener, yoksa DB'deki `name`'e düşer. Görsel yön ve tam token seti kökteki **`DESIGN.md`**'de. | Kullanıcı talebi (2026-08-03): frontend'in sıfırdan yeniden tasarımı; "tek bir token seti hem gölgeli-canlı pazarlamayı hem düz-nötr admin'i taşıyamaz". Mevcut mimarinin admin'i tenant temasıyla boyaması Aşama A araştırması sırasında keşfedildi ve kararın gerekçesini güçlendirdi. |
| D89 | **Performans bütçesi ölçüldü; Sentry bilinçli olarak her yüzeyde bırakıldı.** `/masa` first-load JS **488 KB gzip** ölçüldü (`.next/diagnostics/route-bundle-stats.json`, 82 rota); en hafif rota bile 238 KB — ortak taban. Tabanın **129 KB gzip'i `@sentry/nextjs`**, yani misafir QR menüsü kafe Wi-Fi'sinde 129 KB hata izleme kütüphanesi indiriyor. Üç seçenek sunuldu (misafir yüzeyinden çıkar / her yerde kalsın, bütçeyi yükselt / tembel yükle); kullanıcı **"her yerde kalsın, bütçeyi yükselt"** dedi — izleme kaybı istenmedi. Bütçe: `/masa` ≤300 KB gzip, LCP ≤2.5s, INP ≤200ms, TBT ≤400ms, CLS ≤0.05. JS'te kazanılamayacağı için telafi **hidrasyon disiplininde**: menü ızgarası Server Component kalır, kart başına client animasyon yoktur, giriş animasyonu CSS'tir, ızgarada framer-motion `layout` animasyonu yasaktır. Ayrıca `product-card.tsx` `<Image unoptimized>` kullanıyor ve `next.config.ts`'te `images.remotePatterns` **hiç yok** — next/image boru hattı tamamen atlanıyor; LCP'yi asıl belirleyen bu (JS değil), Faz 21 Adım 0'ın önkoşulu olarak düzeltilir. | Kullanıcı kararı (2026-08-03). |
| D89a | **REVİZE — D89'un 300 KB bütçesi 390 KB'ye çekildi; buna karşılık `src/lib`'de tek bir cerrahi düzeltmeye izin verildi.** Dikey dilim uygulanırken 300 KB'nin Sentry ortak tabanda kalırken **matematiksel olarak ulaşılamaz** olduğu ortaya çıktı: 129 KB Sentry + ~109 KB React/Next runtime + 63 KB supabase-js zaten 301 KB, uygulama kodu sıfır olsa bile taban 376 KB. Aynı ölçümde 63 KB'lık gerçek bir kaçak bulundu: `rating-prompt.tsx` (client) iki satırlık saf `shouldRedirectToGoogle` fonksiyonunu `lib/ratings/schemas.ts`'ten DEĞER olarak import ediyordu; o dosyanın ilk satırı `import { z } from "zod"` olduğu için zod'un tamamı misafirin telefonuna iniyordu. Fonksiyon bağımlılıksız `lib/ratings/threshold.ts`'e taşındı, `schemas.ts`'ten geriye dönük uyumluluk için yeniden export edildi. Bu, Faz 21 brief'indeki "`src/lib/**` dokunulmaz" sınırının bilinçli ve dar kapsamlı tek istisnasıdır — iş mantığı değişmedi, yalnızca saf bir fonksiyon zod'suz bir modüle taşındı. Sonuç: `/masa` 488 → **387 KB gzip** (-%21). Sentry misafir yüzeyinde kalmaya devam ediyor (D89 değişmedi). | Kullanıcı kararı (2026-08-03): "zod kaçağını düzelt, bütçeyi 390 KB'ye çek" — sınırı bir kez, dar kapsamda açmak tercih edildi. |
| D90 | **Demo seed'i 3 katına çıkarıldı; "ekleyici ve tekrarsız" iki kalıcı kural oldu.** acme 2 kategori / 3 ürünle yeni önyüzün yoğunluk ritmini göstermeye yetmiyordu (kanban KDS, kategori şeridi, admin listeleri boş görünüyordu). acme kategori 2→6, ürün 3→16, varyant 2→7, ekstra 2→7, masa 6→13, rezervasyon 2→6, malzeme 2→8, reçete 0→4; beta ürün 1→5; gamma dokunulmadı (taze onboarding demosunun kapsamı). **Kural 1 — ekleyici:** mevcut satırların id/ad/`display_order`'ı değiştirilmez; 11 E2E dosyası `Filtre Kahve`ye, `menu-reorder.spec.ts` `İçecekler`in `Tatlılar` ile KOMŞU olmasına bağlı, bu yüzden yeni kategoriler `display_order` 2'den başlar ve `Filtre Kahve` bilerek varyantsız/ekstrasız kalır. **Kural 2 — ad tekrarı yok:** ne tenant içinde ne tenant'lar arasında; gamma'nın ürünü acme'ninkiyle aynı addaydı, `Gamma Kahve` oldu. Bir de **isimlendirme tuzağı** belgelendi: yeni masalar `Masa 10`.. diye adlandırılamaz, çünkü Playwright'ta `getByRole("option", { name: "Masa 1" })` varsayılan olarak ALT DİZE eşler ve `Masa 10` strict mode ihlali verir — genişletme bunun yerine bölge adlarıyla yapıldı (`Bahçe 1-3`, `Teras 1-2`), bu aynı zamanda `table_zones`'u da demo edilebilir kıldı. Seed sayısına bağlı 5 RLS assertion'ı yeni sayılara güncellendi; izolasyon assertion'larına (`every(...)`, 0-satır) dokunulmadı — sayı "eksik satır" ucunu, `every` "sızan satır" ucunu korur. | Kullanıcı talebi (2026-08-03): "demo verilerimizin de kalitesi ve sayısını 3 katına arttıralım, en önemlisi tekrar eden veriler olmasın". |
| D91 | **Admin listeleri tek bir `DataTable` primitifine alındı; "tek DOM, iki düzen" kuralı ve tablo sıralamasında "ham değerle sırala" kuralı kalıcı oldu.** Kullanıcı talebi üç ayrı sorun gibi görünüyordu (duyarlılık kusurları, boş pano, tablo eksikliği) ama **ölçüm tek kök neden gösterdi**: admin listeleri `flex items-center justify-between` ile masaüstü için yazılmış, `flex-wrap`/`min-w-0` yok. `scripts/responsive-audit.mjs` (22 sayfa × 390/768px) belge düzeyinde yatay taşmanın **hiçbir sayfada olmadığını**, kusurun satır içi **kırpılma** olduğunu gösterdi: `/admin/ingredients` 308px kutuda 508px içerik, `/admin/tables` 324px'te 455px, ata `overflow-hidden` olduğu için taşan kısım kaydırılamıyor bile — telefonda bir masanın QR'ını yenilemek **imkânsızdı**, buton hiç çizilmiyordu. **Karar:** yalnızca `<table>` render eden tek bir bileşen; `sm` altında aynı işaretleme CSS ile etiketli karta dönüşür (`td::before { content: attr(data-label) }`). İkinci bir mobil işaretleme YAZILMAZ — aynı `data-testid` iki kez basılır ve Playwright strict-mode ihlali üretir. **Sıralama her zaman ham değerle** yapılır (kuruş tam sayısı, ISO tarih, dakika, oran); biçimlenmiş metni sıralamak "₺90 > ₺1.200,00" gibi sessiz yanlışlar üretir. Satır altı açılan paneller `expandedRow` ile ayrı bir `<tr>`'de yaşar; kart modunda `:has(+ tr[data-expansion])` ile üstteki karta yapışır (CSS'te "önceki kardeş" seçilemediği için tek yol). Dokunma hedefleri **yalnızca kaba işaretçide** ≥40px'e sabitlenir — masaüstü yoğunluğu DESIGN.md'nin bilinçli tercihi, `.ops-surface`'ın işaretçiden bağımsız 44px'i ise duvara asılı KDS için orada kalır. **Tabloya alınmayanlar bilinçli:** `settings` (form), `menu` (sürükle-bırak sıralı liste — tablo dnd ile kavga eder), **personel ÜYE listesi** (profiles'ta ad/e-posta kolonu yok, tablonun kimlik kolonu olamazdı — cihaz listesi tabloya alındı, üye düzenleyicileri form kaldı). Sonuç: 22 sayfa × 2 genişlikte **0 kırpılma / 0 taşma / 0 bindirme**. | Kullanıcı talebi (2026-08-04): "repossive de sorun var… tablolardada fonsiyonelite önemli o yüzden tabloya uygun olanları tabloya alalım". |
| D92 | **Pano "her sayı gerçek sorgudan" kuralıyla yeniden yazıldı; demo trendi için seed'e 14 günlük KAPANMIŞ gün geçmişi eklendi.** Önceki `/admin` tek bir kart gösteriyordu (işletme adı) ve 33 sayfalık bir panelin giriş ekranı olarak hiçbir soruya cevap vermiyordu. Yeni pano dört soruyu cevaplar: bugün nasıl gidiyor (KPI şeridi, her kart rakamın DAYANDIĞI rapora tarih parametresiyle gider) · şu anda ne oluyor (dolu masa / bekleyen çağrı / mutfak yükü) · neye bakmam gerekiyor (dikkat listesi — panoyu dekoratif olmaktan çıkaran asıl kısım) · sık yaptığım işler nerede. **Uydurma yasağı burada üç somut kurala indi:** kapanmış gün yoksa sparkline HİÇ çizilmez (sıfırla doldurulmaz), `reports.revenue` izni yoksa ciro/ortalama sepet/çok satanlar hiç render edilmez (RULES #41), kapalı modülün bölümü hiç çizilmez (RULES #34). **Ölçümle çıkan iki tuzak:** (1) `get_period_revenue_report` yalnızca `daily_sales_summary`yi okur, oraya da yalnızca kapatılmış günler girer — bugünün sipariş sayısı için kullanılsaydı kasada sipariş varken panoda "0" yazardı; bunun yerine son 48 saat çekilip her satırın tarihi tenant saat diliminde biçimlendiriliyor (yaz saati aritmetiği yok, yeni RPC/migration da yok). (2) Ortalama sepetin paydası "açık sipariş" sayısı olamaz — servis edilen siparişler paydadan düşerken ciro düşmediği için rakam gün ilerledikçe şişerdi. **Seed'e 14 günlük kapanış eklendi** çünkü `daily_sales_summary`deki 6 satırın altısı da E2E'nin `test-s9-day-close-*` tenant'larınındı ve acme'nin hiç kapanışı yoktu. Kritik kısıt: **bugün asla kapatılmaz** — `is_business_date_closed` bugüne bakar, kapatmak "Günü Kapat"ı gizler ve o güne ödeme kabulünü durdururdu. Tarihler `current_date`e göreli üretilir, böylece `db reset` sonrası geçmiş taze kalır. | Kullanıcı talebi (2026-08-04): "Pano ya mevcut sistem ile ilgili… bir çok istatislik veri ekleyelim ki panomuz zengin görünsün… mevcutta panonun boş olmaması lazım bunu bir araştır". |
| D93 | **E2E artıkları spec başına değil TEK bir global teardown'da temizlenir; silinemeyen kayıtlar arşivlenir.** Paket acme'nin GERÇEK demo verisine yazıyor ve spec'lerin çoğu bıraktığını toplamıyordu. Ölçüm (2026-08-04): 60 masanın 47'si zaman damgalı çöp, 2 mükerrer bölge, KDS'de kalıcı açık siparişler, 23 sızmış `test-*` tenant. **Zarar kozmetik değil:** `menu-reorder` ve `session-panel` bu birikme yüzünden kırılıyordu, `schema-and-seed` integration testinin dört beklentisi birden düşüyordu ve pano "60 aktif masa / 19 bekleyen çağrı" gibi anlamsız sayılar gösteriyordu. Spec başına `afterAll` yerine global teardown seçildi çünkü sekiz dosyaya dağıtılan temizlik yeni spec eklenince unutulur; kendi verisini yaratan spec'ler (`menu-crud`) kendi `afterAll`'unu korur. **Silme ölçütü dar:** masa etiketinde 13 haneli `Date.now()`, tenant slug'ında `test-` öneki — seed etiketleri ("Masa 1", "Bahçe 2", "Tezgâh") ve seed tenant'ları (acme/beta/gamma) bu desenlerle asla eşleşmez; yerel olmayan bir Supabase'e karşı teardown hiç çalışmaz. **İlk sürüm sessizce başarısızdı ve ölçümle yakalandı:** `table_sessions.table_id` FK'si `ON DELETE RESTRICT` (sipariş geçmişi değişmez, RULES #36), yani bir kez oturum açılmış masa silinemiyor — teardown "47 masa silindi" diye rapor ederken sayı 60'ta kalmıştı, çünkü `delete()`'in hatası hiç okunmuyordu. Doğru davranış ürünün kendi modeli: **geçmişi olan masa arşivlenir, olmayan silinir**; artık tüm çağrıların hatası kontrol edilir. Buna bağlı olarak `tables.rls.test.ts` sayımı AKTİF satırlara bağlandı — arşiv birikmesi kalıcı olduğu için toplam satır sayısı artık seed'i koruyan bir ölçü değil (sızıntı ucunu `every(...)` korumaya devam ediyor, D90'daki ayrım). | Kullanıcı talebi (2026-08-04): "bırakma temizle". |
| D94 | **`profiles.full_name` eklendi (0091); personel listesi ancak bundan sonra tabloya alınabildi.** Faz 23'te 26 admin listesi tabloya alınırken personel ÜYE listesi dışarıda kalmak zorunda kalmıştı: `profiles` yalnızca rol, rozet no, PIN ve durum tutuyordu, e-posta ise `auth.users`'ta ve RLS altında okunamıyor — yani satırın **kimlik kolonu yoktu**. Somut sonuç: 8 personel yalnızca rozet numarasıyla ayırt ediliyordu ve rozet opsiyonel olduğu için rozetsiz bir personel hiç ayırt edilemiyordu; üstelik liste 8 personeli 8 AÇIK FORM olarak çiziyordu. Kolon **nullable**: D87 ile açılmış mevcut personelin adı yok ve migration onları geçersiz kılmamalı; UI ada düşemediğinde rozete, o da yoksa role düşer ve bunun türetilmiş bir etiket olduğunu soluk renkle belli eder (uydurma yok). Yeni personel oluştururken ad **zorunlu** — aynı açığı tekrar üretmemek için. `update_staff_member`'ın 5 argümanlı sürümü eklendi ve **4 argümanlı eski sürüm silinmedi**: migration ile uygulama dağıtımı arasındaki pencerede eski kod hâlâ onu çağırabilir (strangler kuralı, RULES #22). Boş dize RPC içinde `NULL`'a çevriliyor ki "adı yok" tek bir biçimde temsil edilsin. Liste artık tablo, düzenleyici satırın altına açılıyor. Üç spec güncellendi (davranış aynı): `staff-management` satırı adıyla bulup "Düzenle" ile açıyor, `staff-scheduling` PIN sıfırlamayı düzenleyicinin içinden yapıyor, `waiter-pin-login` yeni zorunlu alanı dolduruyor. İzin matrisi locator'ı kartla kapsandı — sayfada artık üç `<table>` var ve `page.locator("table")` yanlış switch'i seçebilirdi. | Kullanıcı onayı (2026-08-04): Faz 23 kapanışında açık bırakılan iki karardan biri. |
| D95 | **Webhook teslimat testinin dış servis bağımlılığı kaldırıldı; hedef aynı Docker ağındaki bir edge function oldu.** `webhooks.integration.test.ts` `httpbin.org`'a gerçek POST atıyordu (pg_net asenkron olduğu için gerçek bir HTTP cevabı olmadan retry mantığı doğrulanamıyor) ve o servis düştüğünde test **ürün hatası yokken** kırılıyordu — 2026-08-04'te ölçüldü: 15 sn'de HTTP 000, integration'daki tek kırık oydu. **`host.docker.internal` çözüm DEĞİL:** isteği `pg_net` atıyor, yani Postgres CONTAINER'ının içinden çıkıyor; o ad yalnızca Docker Desktop'ta çözülüyor (DB container'ının `/etc/hosts`'unda kayıt yok), CI ise `ubuntu-latest` (.github/workflows/ci.yml) ve orada çözülmez. Ağ içinde 2xx dönen hazır bir uç da yok (ölçüldü: `rest:3000` 405, `auth:9999/health` 405, `kong:8000/` 404). Çözüm `supabase/functions/webhook-test-sink`: Kong üzerinden `http://kong:8000/functions/v1/webhook-test-sink`, `?status=` ile dönülecek HTTP kodu seçiliyor, `verify_jwt = false` (isteği imza başlığı dışında kimlik taşımayan pg_net atıyor). DB container'ından doğrulandı: 200 ve 500. **Operasyonel not:** edge function'ları `supabase start` mount ediyor — yeni bir function eklendikten sonra yerel yığın bir kez yeniden başlatılmalı (veri korunur; ölçüldü: öncesi/sonrası 3 tenant, 49 masa, 79 storage nesnesi). `supabase/functions/**` hem `tsconfig` hem `eslint` dışında bırakıldı (Deno runtime, Node/Next kurallarıyla denetlenmesi anlamsız). Sonuç: integration **438/438**. | Kullanıcı onayı (2026-08-04): Faz 23 kapanışında açık bırakılan iki karardan biri. |

## Oturum 11 — Pazarlama Ana Sayfası (2026-08-06)

| # | Karar | Gerekçe |
|---|---|---|
| D96 | **`plans.is_public` (migration 0092): "bir plan SATIN ALINABİLİR mi" ile "bir plan VİTRİNDE mi" ayrı sorular oldu.** Süper Admin `create_plan` ile "Demo" gibi bir plan açtığında o plan pazarlama ana sayfasının fiyat tablosuna da düşüyordu — tek satır iki soruyu birden cevapladığı için demo/iç plan eklenir eklenmez vitrin bozuluyordu. Kolon varsayılanı `true` (mevcut üç plan vitrindeydi, migration onları gizlememeli); `key in ('demo','trial','internal')` olanlar `false`'a çekiliyor — eşleşme **`key` üzerinden**, çünkü `name` Süper Admin'den serbestçe düzenlenebilirken `key` 0074'te bilinçli olarak değiştirilemez bırakılmıştı. Filtre **veri katmanında**: `getMarketingPlans()` `.eq("is_public", true)` uygular, `getPlans()` (kayıt formu + plan ataması) bilerek filtresizdir. `create_plan`/`update_plan`'ın 7 argümanlı sürümleri eklendi, **6 argümanlı eskiler silinmedi** (strangler kuralı, RULES #22; farklı arite olduğu için overload belirsiz değil). Şema `z.coerce.boolean()` DEĞİL `z.boolean()` kullanır: `coerce` boş olmayan her dizeyi truthy saydığı için `"false"` değerini `true`'ya çevirir ve gizlenmiş bir planı sessizce vitrine geri koyardı. Süper Admin plan kartında yalnızca gizli planlarda çıkan bir rozet var (istisna görünür olsun); kaydetme `/platform/plans` ile birlikte `/`'yi de tazeler, aksi halde bayrak kaydedilir ama ziyaretçi eski listeyi görmeye devam ederdi. Fiyat ızgarasının sabit `sm:grid-cols-3`'ü plan sayısından türetilir hâle geldi. Yerel `seed.sql`'e `is_public = false` bir "Demo" planı eklendi — üretimde bu planı Süper Admin açmıştı, lokalde hiç yoktu ve davranışın testi yazılamıyordu. | Kullanıcı talebi (2026-08-06): "Planlar kısmında anasayfada Demo isimli plan görünmesin, Demo sadece kayıt formunda olsun." |
| D97 | **Pazarlama sayfasının hareketi saf CSS; kaydırmaya bağlı belirme `@supports` ile korunuyor.** DESIGN.md hareket politikası framer-motion'ı yalnızca gerçekten etkileşimli yaprakta serbest bırakıyor, giriş/stagger animasyonlarını CSS'e ve 0 KB JS'e bağlıyor. Ana sayfa bütünüyle Server Component; bir IntersectionObserver sarmalayıcısı eklemek sayfaya `"use client"` sokacaktı. Bunun yerine `animation-timeline: view()` (kaydırmaya bağlı CSS animasyonu) kullanıldı. **`@supports` guard'ı isteğe bağlı değil, güvenlik ağı:** blok `fill-mode: both` ile çalışıyor, yani öğeler animasyon aralığından ÖNCE `opacity: 0`. Özelliği desteklemeyen bir tarayıcıda guard olmasaydı içerik sonsuza kadar görünmez kalırdı — bir animasyon hatası, sayfanın boş görünmesine dönüşürdü. Guard'la birlikte desteklenmeyen tarayıcıda blok hiç uygulanmaz ve her şey normal görünür. `prefers-reduced-motion` iki yerden karşılanıyor: mesafe `--move-md` token'ından geliyor (token azaltma altında zaten `0px`), ama süre token'ı kaydırmaya bağlı animasyonda okunmadığı için `@supports` bloğu İÇİNDE ayrıca `animation: none` yazılıyor — base katmanındaki genel `animation-duration: .01ms` bu animasyon türünü durdurmuyor. Hover geri bildirimi yalnızca `transform` + `box-shadow` (ikisi de compositor'da, düzen hesabı yok) ve `@media (hover: hover)` altında. **Bilinen ve kabul edilen sonuç:** tam sayfa ekran görüntüsü/yazdırma kaydırma olmadan alındığı için kıvrımın altı boş çıkar — gerçek kullanıcı için değil, araçlar için bir etki. | Kullanıcı talebi (2026-08-06): "sayfada animasyonlar eksik, animasyonlarla daha çekici bir hale getirelim." |
| D98 | **"Referanslar" isteği işletme TİPLERİ bölümüne dönüştürüldü — uydurma iddia kuralı korundu.** Kullanıcı uydurma kafe/restoran markalarından oluşan bir referans bölümü istedi; CLAUDE.md ve DESIGN.md referans/testimonial, müşteri logosu ve kaynağı olmayan iddiayı açıkça yasaklıyor ve "marka adı gerekiyorsa kullanıcıya sorulur" diyor. Üç seçenek sunuldu (işletme tipleri / dummy marka + görünür "temsilî" notu / dummy marka + production'da kapalı env bayrağı); **kullanıcı işletme tiplerini seçti**. Sonuç: "Kimler kullanıyor" bölümü altı işletme türünü anlatır, tek bir uydurma isim içermez, kural değişmedi ve gerçek müşteriler geldiğinde bölüm olduğu gibi kalıp altına logo şeridi eklenebilir. Bir E2E testi bölümün sonradan referans/testimonial'a dönüşmesini engelliyor (`/referans/i` ve `/müşterilerimiz/i` sayfada 0 eşleşme). | Kullanıcı seçimi (2026-08-06), çakışma kendisine gösterildikten sonra. |
| D99 | **ÜRETİM OLAYI + kalıcı kural: bir vitrin filtresinin başarısızlığı, ürünün fiyatlarının kaybolmasına dönüşmemeli.** D96'nın `is_public` filtresini içeren uygulama kodu `main`'e push edildi; `main` D83 gereği otomatik olarak Vercel production'a çıkıyor. **Migration 0092 üretim Supabase'ine gitmedi** — CI migration push etmiyor (`.github/workflows/*.yml`'de `supabase db push` yok), yani şema yalnızca elle güncelleniyor. Sonuç: `.eq("is_public", true)` olmayan bir kolona uygulandı, PostgREST isteği reddetti, `getMarketingPlans()` `error`'u hiç okumadığı için `data` null → boş liste → **ana sayfanın fiyat tablosunda tek bir plan kartı kalmadı** (bölüm başlıktan doğrudan lisans kutusuna atlıyordu). Kullanıcı bildirdi, `curl` ile doğrulandı. **İki ayrı hata vardı ve ikisi de düzeltildi:** (1) *sıralama hatası* — şemaya bağımlı uygulama kodu, şema oradayken değil önce deploy edildi; (2) *dayanıklılık hatası* — Supabase istemcisinde `error` okunmadığında başarısız sorgu "boş sonuç"tan ayırt edilemiyor ve arayüz sessizce boşalıyor. Artık `getMarketingPlans()` `error` durumunda filtresiz listeye düşüyor ve log'luyor: fazla plan göstermek, hiç plan göstermemekten iyidir; migration yerine oturduğunda filtre kendiliğinden devreye giriyor. Bir E2E testi fiyat tablosunun asla boşalmamasını koruyor. **Kural:** şemaya bağımlı bir okuma eklenirken ya migration önce üretime uygulanır ya da okuma kolonun yokluğuna dayanıklı yazılır — ve `data` ile birlikte `error` de her zaman okunur. | Üretimde ölçülerek bulundu (2026-08-06). |
| D100 | **Entegrasyon şeridi hero'nun altındaki eski yerine geri alındı — sorun konumu değil kutunun içiydi.** Faz 24'te şerit "ziyaretçi ürünü tanımadan marka adı görmesin" gerekçesiyle modül vitrininin altına taşınmıştı. Kullanıcı geri istedi: sayfadaki yeri doğruymuş, rahatsız eden şey kutunun İÇİNDEKİ düzenmiş — yedi marka adı başlıkla aynı `flex-wrap` satırında yarışıyor, 390px'te düzensiz sarıp satır başları hizasız kalıyordu ("kayma"). Düzeltme kutunun içinde kaldı: adlar kendi satırına ve başlığın biraz altına indi; dar ekranda `flex-wrap`'in sarma kararı yerine **eşit hücreli iki sütunlu ızgara** kullanılıyor, böylece her isim aynı genişlikte bir hücrede ve hizada duruyor, `sm`'den itibaren tek satırlık akışa dönüyor. Taşınma sırasında eklenen tür grupları (Pazar yerleri / Ödeme / Muhasebe) geri alındı — şeridin kompakt hâli kullanıcının onayladığı biçim. E2E bölüm SIRASINI da doğruluyor (şerit modül vitrininin ÜSTÜNDE), böylece bir daha sessizce yer değiştiremez. | Kullanıcı düzeltmesi (2026-08-06): "yeri iyiydi… kendi kutucuğunun içinde biraz daha aşağı… küçük ekranda da kayma yapıyordu". |
